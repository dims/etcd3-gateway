# ref: https://docs.travis-ci.com/user/languages/python
language: python
sudo: true
services:
  - docker
env:
  - TOXENV=pep8
  - TOXENV=py27
  - TOXENV=py35
  - TOXENV=cover
  - TOXENV=docs
  - TOXENV=examples
  - TOXENV=releasenotes

install:
  - pip install tox

script:
  - tox
